<div class="top_bar">
    <div class="top_item">
        <button onclick="showMenu()"> Menu </button>
        <%= link_to "Lanci", launches_path, action: :get, class: :link %>
    </div>
    <div class="logo" onclick="window.location.href = '/articles' ">
        <h1>Spacewalk</h1>
    </div>
    
    <div class="top_item">
    <%= if can? :create, Article 
        link_to Notification.where(admin: current_user).count, proposals_path
    end %>
    </div>
</div>

<%= javascript_tag "function showMenu(){ $('.sidemenu').toggleClass('visible') }"%>